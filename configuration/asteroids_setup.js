import Asteroid from '@/components/sections/technologies/asteroid/utils/AsteroidClass';

const asteroids = [new Asteroid('asteroid-whole-1', { x: 900, y: 200 }, 'git'), new Asteroid('asteroid-whole-2', { x: 100, y: 400 }, 'react')];

export default asteroids;
